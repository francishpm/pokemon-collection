# Frontend do ColecionaDex

Interface Next.js do ColecionaDex. As instruções completas, variáveis de ambiente
e processo de migrations estão no `README.md` da raiz do repositório.

## Comandos

```bash
npm run dev
npm run lint
npm run test
npm run build
```

O servidor local fica disponível em `http://localhost:3000`.
